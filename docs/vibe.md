## 任务目标
书架过滤增加选项,可以过滤 作者 类型 状态 (第一个cbz符合就行)

## 项目背景
- 语言/框架：nfofetch: FastAPI + HTMX
- 包管理：uv
- 质量工具：pytest, mypy, ruff, eslint (前端)
- 部署工具: docker compose v2

## 执行约束
1. **沟通语言**：中文。
2. **前置评估**（回答 3 个问题后再提交方案）：
   - 影响范围？有无副作用？
   - 最优解？替代方案利弊？
   - 测试计划？（新增/修改哪些 case？）
3. **确认拦截**：方案需经我确认，方可实施编码。
4. **代码质量**：
   - Python：新增/修改函数必须有对应 pytest 用例,且全量通过 mypy+ruff。最后全量通过所有pytest,防止出现回归错误。
   - 前端：通过 eslint，并手动验证关键交互。
   - 代码改动,对应的注释(若有)必须一并修正
5. **后置处理**：修改完成后，检查并更新 `AGENTS.md`（只针对 AI 无法从源码和知识图推断的硬约束。）
6. **不确定时**：务必追问，禁止猜测。
7. **可选**：如需分析源站，可请求浏览器自动化工具。




**目标：**
刮削预览时,选择poster应该同时更新预览封面的展示

**输入：**
当前文件夹是一个uv管理虚拟环境的python工程nfofetch: FastAPI + HTMX 影片刮削工具，从 javdb 抓取信息并生成 Jellyfin 兼容的 NFO/图片。

**步骤：**
如有必要,请调用browser-automation分析真实页面
修改前先评估修改的合理性与可行性
修改后的代码如果涉及python部分,必须有完整的pytest单元测试,并通过mypy和ruff检测
前端部分也要进行测试语法测试

每次修改后,必须评估AGENTS.md 和项目的skill(.opencode/skills) 是否需要勘误或者更新
如果有任何问题,请使用提问的方式帮助我确认需求。
不要猜测我的意图。任何不明确的地方都必须都向我提问。

---

**目标：**
文件浏览器增加查看按钮,可以查看文件

**输入：**
当前文件夹是一个uv管理虚拟环境的python工程nfofetch: FastAPI + HTMX 影片刮削工具，从 javdb 抓取信息并生成 Jellyfin 兼容的 NFO/图片。

**步骤：**
如有必要,请调用mcp分析真实页面
修改前先评估修改的合理性与可行性
修改后的代码如果涉及python部分,必须有完整的pytest单元测试,并通过mypy和ruff检测
前端部分也要进行测试语法测试

每次修改后,必须评估AGENTS.md 和项目的skill(.opencode/skills) 是否需要勘误或者更新
如果有任何问题,请使用提问的方式帮助我确认需求。
不要猜测我的意图。任何不明确的地方都必须都向我提问。


---

**目标：**
jav321 和 javhoo的搜索是不是有问题,比如番号 XS-2298 网站上有,却没有搜索到.设置里我已经打开这两个站了


**输入：**
当前文件夹是一个uv管理虚拟环境的python工程nfofetch: FastAPI + HTMX 影片刮削工具，从 javdb 抓取信息并生成 Jellyfin 兼容的 NFO/图片。

**步骤：**
修改前先评估修改的合理性与可行性
修改后的代码如果涉及python部分,必须有完整的pytest单元测试,并通过mypy和ruff检测
前端部分也要进行测试语法测试

每次修改后,必须评估AGENTS.md 和项目的skill(.opencode/skills) 是否需要勘误或者更新
如果有任何问题,请使用提问的方式帮助我确认需求。
不要猜测我的意图。任何不明确的地方都必须都向我提问。


---

**目标：**
仔细分析搜索刮削写入流程,看看有没有潜在的问题

**输入：**
当前文件夹是一个uv管理虚拟环境的python工程nfofetch: FastAPI + HTMX 影片刮削工具，从 javdb 抓取信息并生成 Jellyfin 兼容的 NFO/图片。

**步骤：**
修改前先评估修改的合理性与可行性
修改后的代码如果涉及python部分,必须有完整的pytest单元测试,并通过mypy和ruff检测
前端部分也要进行测试语法测试

每次修改后,必须评估AGENTS.md 和项目的skill(.opencode/skills) 是否需要勘误或者更新
如果有任何问题,请使用提问的方式帮助我确认需求。
不要猜测我的意图。任何不明确的地方都必须都向我提问。


---

**目标：**
功能改进和新增讨论.
刮削预览已经读取所有图片了,后续是不是不用下载,直接从临时文件(不确定)加载?

**输入：**
当前文件夹是一个uv管理虚拟环境的python工程nfofetch: FastAPI + HTMX 影片刮削工具，从 javdb 抓取信息并生成 Jellyfin 兼容的 NFO/图片。

**步骤：**
修改前先评估修改的合理性与可行性
修改后的代码如果涉及python部分,必须有完整的pytest单元测试,并通过mypy和ruff检测
前端部分也要进行测试语法测试

每次修改后,必须评估AGENTS.md 和项目的skill(.opencode/skills) 是否需要勘误或者更新
如果有任何问题,请使用提问的方式帮助我确认需求。
不要猜测我的意图。任何不明确的地方都必须都向我提问。
