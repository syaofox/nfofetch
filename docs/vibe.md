**目标：**
刮削网站增加[jav321](https://www.jav321.com/)
用mcp工具分析

**输入：**
当前文件夹是一个uv管理虚拟环境的python工程nfofetch: FastAPI + HTMX 影片刮削工具，从 javdb 抓取信息并生成 Jellyfin 兼容的 NFO/图片。

**步骤：**
如有必要,请调用mcp分析真实页面
修改前先评估修改的合理性与可行性
修改后的代码如果设计python部分,必须有完整的pytest单元测试,并通过mypy和ruff检测
前端部分也要进行测试语法测试

每次修改后,必须评估AGENTS.md 和项目的skill(.opencode/skills) 是否需要勘误或者更新
如果有任何问题,请使用提问的方式帮助我确认需求。
不要猜测我的意图。任何不明确的地方都必须都向我提问。

---

**目标：**
文件浏览器增加查看按钮,可以查看文件

**输入：**
当前文件夹是一个uv管理虚拟环境的python工程nfofetch: FastAPI + HTMX 影片刮削工具，从 javdb 抓取信息并生成 Jellyfin 兼容的 NFO/图片。

**步骤：**
如有必要,请调用mcp分析真实页面
修改前先评估修改的合理性与可行性
修改后的代码如果设计python部分,必须有完整的pytest单元测试,并通过mypy和ruff检测
前端部分也要进行测试语法测试

每次修改后,必须评估AGENTS.md 和项目的skill(.opencode/skills) 是否需要勘误或者更新
如果有任何问题,请使用提问的方式帮助我确认需求。
不要猜测我的意图。任何不明确的地方都必须都向我提问。
